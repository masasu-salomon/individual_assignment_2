import 'package:flutter/material.dart';

/// App-wide colors and constants matching the design (dark blue, yellow accent).
class AppConstants {
  static const Color primaryDark = Color(0xFF0F1C3A);
  static const Color accentYellow = Color(0xFFFFC107);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color chipUnselectedBg = Color(0xFFF5F5F5);
  static const Color scaffoldBackground = Color(0xFFFFFFFF);

  static const double appBarElevation = 0;
  static const double cardElevation = 1;
  static const double cardRadius = 12;
}
